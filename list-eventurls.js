const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const eventUrls = await prisma.eventUrl.findMany(); console.log('All EventURLs in database:'); eventUrls.forEach(e => console.log({ id: e.id, userId: e.userId, urlPath: e.urlPath, eventName: e.eventName })); await prisma.$disconnect(); } main().catch(e => { console.error(e); process.exit(1); });
