const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const userId = 'cmaqyjawq000rj0b2d52mp98g'; try { console.log('Creating settings for user:', userId); const newSettings = await prisma.settings.create({ data: { userId, eventName: 'Test Event', adminEmail: 'test@example.com', smtpHost: 'smtp.example.com', smtpPort: 587, smtpUser: 'user', smtpPassword: 'password', customJourneyEnabled: true, captureMode: 'video', companyName: 'Test Company' } }); console.log('Successfully created settings:', newSettings.id); } catch (error) { console.error('Error creating settings:', error.message); } finally { await prisma.$disconnect(); } } main();
