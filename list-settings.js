const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const settings = await prisma.settings.findMany(); console.log('All settings in database:'); settings.forEach(s => console.log({ id: s.id, userId: s.userId, eventName: s.eventName, captureMode: s.captureMode, customJourneyEnabled: s.customJourneyEnabled })); await prisma.$disconnect(); } main().catch(e => { console.error(e); process.exit(1); });
