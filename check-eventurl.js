const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const eventUrlId = '87543205-3280-11f0-97e7-ac1f6bbcd62c'; const eventUrl = await prisma.eventUrl.findUnique({ where: { id: eventUrlId } }); console.log('Event URL:', eventUrl); if (eventUrl && eventUrl.userId) { const userSettings = await prisma.settings.findFirst({ where: { userId: eventUrl.userId } }); console.log('User Settings for EventURL userId:', userSettings); } await prisma.$disconnect(); } main().catch(e => { console.error(e); process.exit(1); });
