const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const userId = 'cmaqyjawq000rj0b2d52mp98g'; const settings = await prisma.settings.findMany(); console.log('Total settings in database:', settings.length); const userSettings = await prisma.settings.findFirst({ where: { userId } }); console.log('User Settings:', userSettings); await prisma.$disconnect(); } main().catch(e => { console.error(e); process.exit(1); });
